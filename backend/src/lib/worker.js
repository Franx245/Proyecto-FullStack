/**
 * BullMQ Worker stub — disabled for Vercel Serverless.
 *
 * Workers require persistent processes.  Exports are safe no-ops.
 */

export function startWorker() {}
export async function stopWorker() {}
